make num = randint(100)

make guess = ask("Guess a number from 0 to 99")

while ( num != guess) {
	make guess = ask("Guess a number from 0 to 99")
	if (num > guess) {
		show("too low!")
	} elif (num < guess) {
		show("too high!")
	}
}

show("You guessed it!")
show("The number was: ")
show(num)